<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.io.cleanisimportant" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleanIsImportant" >

        <activity
            android:name=".ScrollActivity"
            android:exported="true" />
        <activity
            android:name=".ApartmentActivity"
            android:exported="true" />
        <activity
            android:name=".ZacazActivity"
            android:exported="true" />
        <activity
            android:name=".PigActivity"
            android:exported="true" />
        <activity
            android:name=".ManActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>